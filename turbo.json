{
	"$schema": "https://turbo.build/schema.json",
	"tasks": {
		"build": {
			"outputs": ["dict/**"]
		},
		"test": {
			"dependsOn": ["build"]
		},
		"prepare:dictionary": {
			"dependsOn": ["build"]
		}
	}
}
